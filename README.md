# Anagram Solver

A Flask web app that finds anagrams for any word.

## How to Run
1. Install Flask: `pip install flask`
2. Run the app: `python main.py`
3. Open your browser and go to `http://127.0.0.1:5000`

## Built With
- Python
- Flask
